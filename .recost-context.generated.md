# ReCost Scan — extension

## Summary
- Generator: ReCost 0.1.0
- Files scanned: 125
- Clusters: 2
- Providers: Anthropic, OpenAI

## Key Risks
- Missing caching on hot path
- Repeated endpoint calls

## Top Files
- src/chat/providers/mistral.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/chat/providers/openai.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/chat/providers/perplexity.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/chat/providers/xai.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- dashboard/src/lib/api.ts — this file contains an API path in the current cluster, but the evidence here is still limited

---

## Cluster 1 — src/chat/providers/xai.ts

### Why this matters
This file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work.

### Providers
Cluster context: Anthropic, OpenAI
Primary file only: Anthropic
Added by related files: OpenAI

### Top Risks
- Potential missing caching on hot path

### Findings
- None

### Related Files
- src/ast/call-visitor.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/chat/providers/mistral.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/chat/providers/openai.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/chat/providers/perplexity.ts — this file makes cache-capable reads without cache evidence, so it may be doing avoidable repeated work
- src/scanner/patterns/provider-anthropic.ts — this file repeats 1 endpoint pattern, which can amplify request volume

### Snippets
```ts
// Cacheable call without cache
};
    if (typeof request.temperature === "number") body.temperature = request.temperature;
    if (typeof request.maxTokens === "number") body.max_tokens = request.maxTokens;
    if (request.stream) body.stream = true;
    return {
      url: `${xAiAdapter.baseUrl}${xAiAdapter.defaultChatEndpoint}`,
      headers: {
```

```ts
// Cacheable call without cache
*
 * Handles:
 * - Simple calls: `fetch("url")`
 * - Member chains: `openai.chat.completions.create(...)`
 * - Awaited calls: `await client.messages.create(...)` (await is transparent)
 * - Optional chaining: `openai?.chat?.completions?.create()` → normalised to dots
 * - Deeply nested chains: `a.b.c.d.e.f()`
 * - Computed properties: chain is truncated at the computed segment
```

```ts
// Cacheable call without cache
};
    if (typeof request.temperature === "number") body.temperature = request.temperature;
    if (typeof request.maxTokens === "number") body.max_tokens = request.maxTokens;
    if (request.stream) body.stream = true;
    return {
      url: `${mistralAdapter.baseUrl}${mistralAdapter.defaultChatEndpoint}`,
      headers: {
```

```ts
// Cacheable call without cache
};
    if (typeof request.temperature === "number") body.temperature = request.temperature;
    if (typeof request.maxTokens === "number") body.max_tokens = request.maxTokens;
    if (request.stream) body.stream = true;
    return {
      url: `${openAiAdapter.baseUrl}${openAiAdapter.defaultChatEndpoint}`,
      headers: {
```

```ts
// Cacheable call without cache
};
    if (typeof request.temperature === "number") body.temperature = request.temperature;
    if (typeof request.maxTokens === "number") body.max_tokens = request.maxTokens;
    if (request.stream) body.stream = true;
    return {
      url: `${perplexityAdapter.baseUrl}${perplexityAdapter.defaultChatEndpoint}`,
      headers: {
```

---

## Cluster 2 — dashboard/src/lib/api.ts

### Why this matters
This file contains an API path in the current cluster, but the evidence here is still limited.

### Providers
None

### Top Risks
- Potential relevant API path

### Findings
- None

### Related Files
- src/api-client.ts — this file contains an API path in the current cluster, but the evidence here is still limited

### Snippets
```ts
// Relevant API call
if (v !== undefined && v !== "") url.searchParams.set(k, String(v));
    }
  }
  const res = await fetch(url.toString());
  return handleResponse<T>(res);
}
```

```ts
// Relevant API call
err.status = 401;
    throw err;
  }
  await apiFetch("/projects?limit=1", undefined, rcApiKey);
}

export interface ScanResult {
```